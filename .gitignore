# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
public/vendor

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python (tests/)
__pycache__/
*.pyc

# 自動生成物（vfs_root/ から scripts/build_defaults.js が作る）
# コミットすると、vfs_root を変えた PR で再生成を忘れたときに
# 「古いコードが入っているように見える」状態になる（実際に403行ずれていた）。
# predev / prebuild が必ず作り直すので、追跡しない。
src/config/default_files.ts
